using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyController : MonoBehaviour
{
    EnemyModel model;

    public void Init(int enemyNumber)
    {
        //model = new EnemyModel(enemyNumber);
    }
}
